# DOC-MAP — {{PROJECT_NAME}} code knowledge index (the KB)

> **What this is:** the curated index of the docs that matter for understanding *this codebase* — one
> line each: what it is and when to read it. A router, not a summary; detail lives in the linked docs.
> The knowledge base itself lives beside this file in `.claudster/kb/`. You may also link any other
> code-relevant doc from here (a key design doc, an API contract) — but this is for **code-relevant**
> docs, not your whole `docs/` folder.
>
> **How to use it:** when a task touches an area, open the doc(s) for it first. Read heavy docs via a
> dispatched subagent that returns only the conclusion, so the main thread's context stays lean.
>
> **Discipline:** kept honest by [`scripts/check_doc_coverage.py`](../../scripts/check_doc_coverage.py)
> at the pre-push gate. Two simple rules: every link here must resolve (a link to a missing file is a
> **hard failure**), and a KB note that isn't indexed here **warns**. It does NOT police your `docs/`
> folder — the KB is the code-relevant set you curate.

## Read first
1. `CLAUDE.md` — project laws + the dev harness. Always loaded.
2. The entries below, by area.

<!--
MAINTAINING THIS FILE
- Add a row per code-relevant doc as a MARKDOWN LINK whose target is relative to this file, e.g.:
      | [domain-model.md](domain-model.md) | The core domain model. |
  KB notes live beside this file in .claudster/kb/. You can also link a doc elsewhere in the repo
  (e.g. ../../docs/architecture/overview.md) — the checker just verifies the link resolves.
- Only .claudster/kb/*.md is "governed": a KB note not linked here warns (a nudge to index it). Your
  wider docs/ folder is the project's own documentation and is NOT checked.
- Remove a row when you delete its doc — a link to a missing file is a HARD failure at the gate.
- The placeholder rows below carry no links on purpose, so a fresh repo passes the gate. Replace them.
-->

## Knowledge base (`.claudster/kb/`)

| Doc | What / when to read |
|---|---|
| _(add your KB notes here as links — see the comment above for the one-line format)_ | |

## Other key code-relevant docs (optional)

| Doc | What / when to read |
|---|---|
| _(optionally link a design doc, API contract, or deploy runbook — linked, never required)_ | |

---

*Governed = `.claudster/kb/*.md` only (must be indexed here). The wider `docs/` tree is the project's
own documentation and is intentionally not policed.*
