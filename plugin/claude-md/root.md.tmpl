# {{PROJECT_NAME}} — Project Memory (root)

> Lean root: project identity, the development harness, and the non-negotiable laws. Deep per-area
> standards live in folder-level `CLAUDE.md` files (`src/`, `frontend/`, `tests/`, …) and load only
> when you work there. Reusable how-to knowledge comes from the **claudster plugin's skills** (invoked by name / auto-triggered when a task matches) — not a per-project folder.
> Codex CLI reads `AGENTS.md`, which mirrors this file.

## What this project is
{{PROJECT_DESCRIPTION}}

**Stack:** {{STACK_SUMMARY}}
{{STACK_REFERENCE_LINE}}

## The Development Harness (how we work)
Every non-trivial change follows this loop. The **plan file is the durable spine** — it survives
session death, so progress is never lost to context limits.

```
1. PLAN      /feature-plan <desc>   → writes .claudster/plans/<feature>.md (phased)
2. per phase, in the MAIN thread:
   a. RED      write the failing test first   (TDD — see Law 1)
   b. GREEN    minimal code to pass
   c. REFACTOR clean up, tests stay green
   d. VERIFY   dispatch subagents (own context, report back):
                 • preflight   — validate plan vs codebase before coding a phase
                 • tester      — author/run tests, return pass/fail
                 • code-review — return verdict + issues
   e. CHECKPOINT  update the plan's tracker → commit → (optionally /compact)
3. END SESSION   /handoff   → writes relay.md so the next session resumes exactly
```

### Why subagents (context-rot defense)
Anything that **reads a lot but writes a little** — codebase audit, review, preflight, test runs,
security scan — runs as a **subagent**. It burns tokens in its own window and returns only a
conclusion, so the main thread stays clean. This is the primary defense against context rot and
session overflow. Dispatch aggressively. (Proven: subagent review catches real defects the main
thread misses, while keeping main-thread context small.)

### Resuming a session
On a fresh session read in order: `relay.md` (if present) → the active plan in `.claudster/plans/` → its
tracker. The conversation is disposable; these files are the truth.

## The Laws (non-negotiable)
1. **TDD always.** No production code without a failing test first. Red → Green → Refactor. A behavior
   change must start with a failing test. Never write the test after the code and call it TDD.
2. **No absolute paths.** Resolve from file/project root. Portable always.
3. **No silent failures.** Specific exceptions, logged. Never swallow errors without a trace.
4. **Verify, don't assume.** Run the build/lint/test before claiming done. State what you ran.
5. **Honest status.** "Partially done, here's what remains" beats a false "done". Report failures
   with their output. If a step was skipped, say so.
6. **Stay in scope.** Do the phase's work and stop at its exit gate. Don't gold-plate.

## Always-active conventions
- **Commits**: Conventional Commits (`feat:`, `fix:`, `test:`, `chore:`). One logical change per commit.
- **Terse**: lead with the answer; results/code over prose.
- **Secrets**: never commit; keep env/config secrets git-ignored. Never print credentials.
- **Document frontmatter**: Every descriptive Markdown deliverable you write (plan, PRD, ADR, design doc,
  runbook, analysis, handoff) must open with a YAML frontmatter block:
  ```yaml
  ---
  type: plan|prd|adr|design|runbook|handoff|analysis|review
  status: draft|current|done|superseded
  feature: <feature-slug>
  creation-agent: claudster
  Original Author: <Claude Code or human name>
  Creation Date: <YYYY-MM-DDTHH:MM:SSZ>
  Creating Model: <exact model ID, e.g. claude-sonnet-4-6>
  ---
  ```
  On update, add/refresh: `Last Author`, `Last Updated`, `Last Model Used`.
  Do NOT add frontmatter to: `relay.md`, `CLAUDE.md`, skill files, or agent config files.

## Where things live
| Need | Location |
|---|---|
| Active plans | `.claudster/plans/<feature>.md` |
| PRDs | `.claudster/prd/<feature-slug>.md` |
| ADRs, design docs, runbooks | `.claudster/adr/`, `.claudster/design/`, `.claudster/runbooks/` |
| Agent-produced docs (evals, reviews, debug) | `.claudster/agent-docs/` |
| Session resume doc | `relay.md` (repo root) |
| Custom commands | `.claude/commands/` |
| Subagents | `.claude/agents/` |
| Reusable skills | claudster plugin (by name; not copied into the project) |
